.DS_Store
build/bin
node_modules
frontend/dist

# Compiled binaries
thawts
thawts-client
thawts-client.exe
bin/

# Wails v3 generated files
# frontend/bindings/ is committed; regenerate locally with: wails3 generate bindings -clean=true
build/darwin/icons.icns

# Task runner build cache
.task/

# ML model weights and native libs (large files, not versioned)
# Run scripts/download_ai_deps.sh to fetch these before building.
valhalla/
typeform/
internal/app/valhalla/
internal/app/typeform/
internal/tray/typeform/
internal/ai/onnx/models/
internal/ai/onnx/libs/

# Local development environment configuration (not versioned)
DEV.md
